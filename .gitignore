# Dart / pub
.dart_tool/
build/
# A library does not commit a lockfile: https://dart.dev/guides/libraries/private-files
pubspec.lock
# Local development against a sibling checkout. Never committed — CI must resolve the git tag.
pubspec_overrides.yaml
coverage/
reports/
.flutter-plugins
.flutter-plugins-dependencies

# The gallery is a full Flutter app; its platform folders carry generated residue.
example/android/.gradle/
example/android/local.properties
example/ios/Pods/
example/ios/.symlinks/
example/ios/Flutter/ephemeral/
example/macos/Flutter/ephemeral/
example/windows/flutter/ephemeral/
example/linux/flutter/ephemeral/

# Editors
.idea/
*.iml
.DS_Store
