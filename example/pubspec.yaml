name: example
description: "Aurora Glass UI Kit gallery: the live Storybook for package:aurora_glass."
publish_to: "none"

version: 1.0.0+1

# Deliberately resolved on its own rather than as part of anything.

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  # The package this app exists to exercise.
  aurora_glass:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  lints_tool:
    git:
      url: https://github.com/zs-dima/lints_tool.git
      ref: v1.0.1

flutter:
  uses-material-design: true
